apiVersion: v1
kind: Service
metadata:
  name: recipie-api-service
  namespace: aaron-final
spec:
  selector:
    app: recipie-api
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: ClusterIP